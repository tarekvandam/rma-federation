import Contact from "@/components/Contact";

export const metadata = {
  title: "Contact | RMA Federation",
  description: "Connect with the RMA Federation team for media, membership, event, and training inquiries.",
};

export default function ContactPage() {
  return <Contact />;
}
